[project]
name = "genshin-lore-db"
version = "0.8.0"
description = "Project Amber v2 corpus pipeline and developer search engine for Genshin Impact lore research"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
